// TODO: Implement PropertyDetailPage
const PropertyDetailPage = ({ params: { slug } }: { params: { slug: string } }) => {
    console.log('slug', slug);

    return <div>PropertyDetailPage</div>;
};

export default PropertyDetailPage;
