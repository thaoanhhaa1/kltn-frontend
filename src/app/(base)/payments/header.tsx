'use client';

import { Typography } from 'antd';

const Header = () => {
    return (
        <Typography.Title
            style={{
                textAlign: 'center',
            }}
            level={3}
        >
            Thanh toán hoá đơn
        </Typography.Title>
    );
};

export default Header;
