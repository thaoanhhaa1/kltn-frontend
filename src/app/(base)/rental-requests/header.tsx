'use client';

import { Typography } from 'antd';

const Header = () => {
    return (
        <Typography.Title
            style={{
                textAlign: 'center',
            }}
            level={3}
        >
            Yêu cầu thuê nhà
        </Typography.Title>
    );
};

export default Header;
